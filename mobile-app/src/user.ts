import { ref } from 'vue'

/*
  {
    title: 'My shelf',
    images: [ xyz, ...]
    boxes: [ xyz, ...]
    books: [{
      text: xyz,
      author: xyz,
      genre: xyz,
      summary: xyz    
    }, ...]
  },
*/

export const userShelfs = ref([])
